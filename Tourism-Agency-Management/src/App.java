import entity.User;
import view.LoginView;
import view.MenuView;
import view.UserView;

public class App {
    // Stock 0 ise save butonu durumu kontrol edip, uyarı verecek
    public static void main(String[] args) {
        LoginView loginView = new LoginView();
    }
}
